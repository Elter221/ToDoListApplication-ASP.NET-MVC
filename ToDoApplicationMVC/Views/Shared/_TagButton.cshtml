@using ToDoApplicationMVC.BLL.Models
@model TagModel

@{
    var id = ViewContext.RouteData.Values["id"]?.ToString();
}

@{
    <form asp-controller="Tag" asp-action="DeleteTag" method="post" class="tag-form">
        <input type="hidden" name="tagId" value="@Model.Id" />
        <input type="hidden" name="id" value="@id" />
        <div class="tag-with-delete">
            <span class="tag-text">@Model.Name</span>
            @if (User.IsInRole("Admin") || User.IsInRole("Owner") || User.IsInRole("Editor"))
                {
                <button type="submit" class="delete-tag-btn">×</button>
                }
        </div>
    </form>
}
